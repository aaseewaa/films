[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -m "not db"
markers =
    unit: модульные тесты (функции сервисов, без БД)
    integration: интеграционные тесты REST API
    db: требует подключения к PostgreSQL из .env
